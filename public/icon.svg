<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
  <defs>
    <!-- Background Clip -->
    <clipPath id="bgClip">
      <rect x="0" y="0" width="200" height="200" rx="44" ry="44" />
    </clipPath>
  </defs>

  <!-- Background: White Rounded Square (macOS Standard) -->
  <rect x="0" y="0" width="200" height="200" rx="44" ry="44" fill="#FFFFFF" />

  <!-- Wireframe Cube from logo2_orange.svg -->
  <!-- Centered and scaled down to ~75-80% to fit nicely in icon -->
  <!-- Original viewbox was 0 0 200 200, so transform center -->
  <g transform="translate(100, 100) scale(0.75) translate(-100, -100)">
    
    <!-- 3D Cube with code symbol -->
    <!-- Used #E16B48 (Terracotta Orange) as requested -->

    <!-- Top face -->
    <path d="M100 20 L170 60 L100 100 L30 60 Z" fill="none" stroke="#E16B48" stroke-width="10" stroke-linejoin="round" />

    <!-- Left face -->
    <path d="M30 60 L100 100 L100 180 L30 140 Z" fill="none" stroke="#E16B48" stroke-width="10"
        stroke-linejoin="round" />

    <!-- Right face -->
    <path d="M100 100 L170 60 L170 140 L100 180 Z" fill="none" stroke="#E16B48" stroke-width="10"
        stroke-linejoin="round" />

    <!-- Code symbol >_ on left face with perspective skew -->
    <g fill="none" stroke="#E16B48" stroke-width="6" stroke-linecap="round" stroke-linejoin="round" transform="matrix(1, 0.577, 0, 1, 0, 0)">
        <!-- > (centered vertically roughly around y=85 in unskewed space) -->
        <path d="M52 78 L62 85 L52 92" />
        <!-- _ (aligned with baseline) -->
        <path d="M67 94 L79 94" />
    </g>
    
  </g>
</svg>
